# 1. Создать виртуальное окружение
python -m venv venv

# 2. Активировать виртуальное окружение
# Для Windows:
venv\Scripts\activate
# Для Linux/Mac:
source venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Установить Tesseract OCR (необходимо для распознавания текста)
# Для Windows: скачать установщик с https://github.com/UB-Mannheim/tesseract/wiki
# Для Linux:
sudo apt-get install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
# Для Mac:
brew install tesseract tesseract-lang

# 5. Установить Poppler (необходимо для pdf2image)
# Для Windows: скачать с https://github.com/oschwartz10612/poppler-windows/releases/
# Для Linux:
sudo apt-get install poppler-utils
# Для Mac:
brew install poppler
Файл README.md
text
# 📚 DocScanner - Локальное приложение для сканирования и управления документами

Полнофункциональное веб-приложение для сканирования, хранения, распознавания текста (OCR) и редактирования документов. Работает локально без подключения к интернету.

## ✨ Основные возможности

- **Сканирование документов** через веб-камеру или загрузка файлов
- **OCR (распознавание текста)** из изображений и PDF с помощью Tesseract
- **Редактор документов** с поддержкой форматирования
- **Организация в папки** для удобной навигации
- **Экспорт** в PDF, TXT, DOCX форматы
- **Адаптивный дизайн** для всех устройств (смартфоны, планшеты, ПК)
- **Многопользовательская система** с раздельным хранением данных
- **Административная панель** для управления пользователями

## 🚀 Быстрый старт

### Требования

- Python 3.8 или выше
- Tesseract OCR
- Poppler (для конвертации PDF)

### Установка

#### 1. Клонируйте репозиторий или скачайте файлы проекта

```bash
cd document_scanner_app
2. Создайте виртуальное окружение
bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
3. Установите зависимости Python
bash
pip install -r requirements.txt
4. Установите Tesseract OCR
Windows:

Скачайте установщик: https://github.com/UB-Mannheim/tesseract/wiki

Установите с поддержкой русского языка

По умолчанию устанавливается в C:\Program Files\Tesseract-OCR\

Linux (Ubuntu/Debian):

bash
sudo apt-get update
sudo apt-get install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
Mac:

bash
brew install tesseract tesseract-lang
5. Установите Poppler (для PDF конвертации)
Windows:

Скачайте: https://github.com/oschwartz10612/poppler-windows/releases/

Распакуйте и добавьте путь к bin в переменную PATH

Linux:

bash
sudo apt-get install poppler-utils
Mac:

bash
brew install poppler
6. Настройте config.py
Откройте config.py и укажите путь к Tesseract (для Windows):

python
TESSERACT_CMD = 'C:\\Program Files\\Tesseract-OCR\\tesseract.exe'
Для Linux/Mac путь обычно /usr/bin/tesseract (по умолчанию).

Запуск приложения
bash
python app.py
Приложение будет доступно по адресу: http://localhost:5000

Первый вход
Администратор по умолчанию:

Имя пользователя: admin

Пароль: admin123

⚠️ ВАЖНО: Измените пароль администратора после первого входа!

📱 Использование
Для пользователей
Регистрация: Создайте аккаунт на странице регистрации

Сканирование:

Используйте камеру устройства для съемки документов

Или загрузите готовые файлы (PDF, JPG, PNG)

Организация: Создавайте папки и сортируйте документы

Редактирование: Исправляйте распознанный текст в встроенном редакторе

Экспорт: Скачивайте документы в нужном формате

Для администраторов
Войдите под учетной записью администратора

Перейдите в раздел Админ в навигационном меню

Управляйте пользователями, просматривайте статистику

🎨 Адаптивный дизайн
Приложение полностью адаптировано для всех устройств:

Смартфоны (320px+): Компактный интерфейс с сенсорным управлением

Планшеты (768px+): Расширенный интерфейс с боковыми панелями

Десктопы (992px+): Полнофункциональный интерфейс

Большие экраны (1400px+): Оптимизированная сетка документов

📁 Структура проекта
text
document_scanner_app/
├── app.py                 # Главный файл приложения
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости Python
├── models/                # Модели базы данных
├── routes/                # Маршруты (контроллеры)
├── services/              # Бизнес-логика
├── utils/                 # Утилиты
├── static/                # CSS, JS
├── templates/             # HTML шаблоны
├── uploads/               # Загруженные файлы (создается автоматически)
├── database/              # База данных SQLite (создается автоматически)
└── logs/                  # Логи приложения (создается автоматически)
🔧 Настройка
Основные настройки в файле config.py:

python
# Размер загружаемых файлов
MAX_CONTENT_LENGTH = 50 * 1024 * 1024  # 50 МБ

# Языки OCR
TESSERACT_LANG = 'rus+eng'

# Размер миниатюр
THUMBNAIL_SIZE = (300, 400)

# DPI для PDF конвертации
PDF_TO_IMAGE_DPI = 300
🛠️ Решение проблем
Tesseract не найден
Ошибка: TesseractNotFoundError

Решение:

Убедитесь, что Tesseract установлен

Проверьте путь в config.py → TESSERACT_CMD

Для Windows используйте полный путь с двойными слешами

Ошибка конвертации PDF
Ошибка: PDFInfoNotInstalledError

Решение:

Установите Poppler

Добавьте путь к Poppler в переменную PATH

Или укажите путь в config.py → POPPLER_PATH

Камера не работает
Решение:

Разрешите доступ к камере в браузере

Используйте HTTPS (для продакшена)

Проверьте, что камера не используется другим приложением

📝 Разработка
Режимы работы
bash
# Режим разработки (с отладкой)
export FLASK_ENV=development
python app.py

# Режим продакшена
export FLASK_ENV=production
gunicorn -w 4 -b 0.0.0.0:5000 app:app
Создание резервной копии
bash
# Копируем базу данных
cp database/app.db database/backup_$(date +%Y%m%d).db

# Копируем загруженные файлы
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz uploads/

📋 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ЗАПУСКУ ПРОЕКТА
ШАГ 1: Установка Python зависимостей
Откройте терминал (PowerShell или CMD) в папке проекта C:\Users\kenes\Desktop\diplom\ и выполните:

bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
Если возникнет ошибка "pip не найден", сначала обновите pip:

bash
python -m pip install --upgrade pip
ШАГ 2: Установка Tesseract OCR (для распознавания текста)
Для Windows:
Скачайте установщик:

Перейдите: https://github.com/UB-Mannheim/tesseract/wiki

Скачайте файл tesseract-ocr-w64-setup-5.3.x.exe (последняя версия)

Установите Tesseract:

Запустите установщик

⚠️ ВАЖНО: При установке отметьте галочки для установки языков:

✅ Russian language data

✅ English language data

Путь установки по умолчанию: C:\Program Files\Tesseract-OCR\

Проверьте установку:

bash
# В терминале выполните
"C:\Program Files\Tesseract-OCR\tesseract.exe" --version
Если команда работает - Tesseract установлен правильно! ✅

ШАГ 3: Установка Poppler (для работы с PDF)
Для Windows:
Скачайте Poppler:

Перейдите: https://github.com/oschwartz10612/poppler-windows/releases

Скачайте последний релиз Release-XX.XX.X-0.zip

Распакуйте архив:

Распакуйте в удобное место, например: C:\poppler\

Внутри должны быть папки bin, include, lib

Добавьте в PATH (опционально, но рекомендуется):

Нажмите Win + R, введите sysdm.cpl

Вкладка "Дополнительно" → "Переменные среды"

В "Системные переменные" найдите Path → "Изменить"

Добавьте: C:\poppler\Library\bin\

Проверьте установку:

bash
C:\poppler\Library\bin\pdftoppm.exe -v
ШАГ 4: Настройка config.py
Откройте файл config.py и найдите строки:

python
# Путь к Tesseract OCR
TESSERACT_CMD = 'C:\\Program Files\\Tesseract-OCR\\tesseract.exe'

# Путь к Poppler (если не в PATH)
POPPLER_PATH = 'C:\\poppler\\Library\\bin'  # или оставьте None, если добавили в PATH
Убедитесь, что пути указаны правильно!

Если Tesseract установлен в другое место, измените путь. Например:

python
TESSERACT_CMD = 'C:\\Tesseract-OCR\\tesseract.exe'
ШАГ 5: Создание необходимых папок
Проект автоматически создаст папки при первом запуске, но можно создать вручную:

bash
mkdir uploads
mkdir database
mkdir logs
ШАГ 6: Запуск приложения
bash
# Убедитесь, что виртуальное окружение активировано
venv\Scripts\activate

# Запустите приложение
python app.py
Вы должны увидеть:

text
 * Running on http://127.0.0.1:5000
 * Running on http://192.168.x.x:5000
ШАГ 7: Открытие в браузере
Откройте браузер (Chrome, Firefox, Edge)

Перейдите по адресу: http://localhost:5000

ШАГ 8: Первый вход
При первом запуске автоматически создается администратор:

Данные для входа:

Имя пользователя: admin

Пароль: admin123

⚠️ ВАЖНО: После входа сразу смените пароль!

Перейдите в "Профиль"

Раздел "Изменить пароль"

Установите новый безопасный пароль

🔧 РЕШЕНИЕ ЧАСТЫХ ПРОБЛЕМ
❌ Ошибка: "Tesseract not found"
Решение:

Проверьте путь в config.py

Убедитесь, что Tesseract установлен

Попробуйте указать полный путь с двойными слешами:

python
TESSERACT_CMD = 'C:\\Program Files\\Tesseract-OCR\\tesseract.exe'
❌ Ошибка: "PDFInfoNotInstalledError"
Решение:

Проверьте, что Poppler распакован

В config.py укажите путь к папке bin:

python
POPPLER_PATH = 'C:\\poppler\\Library\\bin'
❌ Ошибка: "Module not found"
Решение:

bash
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
❌ Порт 5000 занят
Решение:

bash
# Запустите на другом порту
set PORT=8000
python app.py
Или измените в app.py последнюю строку:

python
app.run(host='0.0.0.0', port=8000, debug=True)
📱 Доступ с телефона (в локальной сети)
Узнайте IP вашего компьютера:

bash
ipconfig
Найдите "IPv4-адрес", например: 192.168.1.100

На телефоне откройте браузер и перейдите:

text
http://192.168.1.100:5000
Важно: Компьютер и телефон должны быть в одной Wi-Fi сети!

✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ
После запуска проверьте:

✅ Вход работает - можете войти под admin/admin123

✅ Загрузка файлов - загрузите тестовый документ

✅ Камера работает (если есть) - попробуйте сделать снимок

✅ OCR работает - загрузите картинку с текстом и проверьте распознавание

✅ Редактор работает - откройте документ и отредактируйте текст

📝 СЛЕДУЮЩИЕ ШАГИ
После успешного запуска:

Создайте тестового пользователя:

Выйдите из admin

Нажмите "Регистрация"

Создайте обычного пользователя

Протестируйте функции:

Создайте папки

Загрузите документы

Попробуйте OCR

Проверьте редактор

Экспортируйте в PDF/DOCX

Измените пароль администратора!